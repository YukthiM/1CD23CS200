//backend
cd notification-app-be
npm install
npm run dev

//frontend
cd notification-app-fe
npm install
npm run dev

the result was obtained for notifications page in frontend using react.js but as part of testing i have also implemented backend locall.

the results obtained are as shown in the snapshots frontend-1.png to frontend-4.png with filters and previous and next buttons.
